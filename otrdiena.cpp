#include <iostream>
#include <cmath>
using namespace std;

int main () {
double x, y;
for (int x = -3; x = 3; x = x + 0.5) {
y = (pow(x,4) + (-2*pow(x,3)) + (x * x) - 1);
cout << x; 
cout << y; 
}


    return 0;
}
